"use client";

import { useRef, useState } from "react";
import { motion, useInView, AnimatePresence } from "framer-motion";

const faqs = [
  {
    q: "What is technical SEO?",
    a: "Technical SEO refers to optimisations that help search engines crawl, index, and understand your website. It includes site speed, mobile usability, security, structured data, and fixing errors that prevent pages from ranking.",
  },
  {
    q: "How do I know if my website has technical SEO problems?",
    a: "Common signs include slow loading times, pages not appearing in Google, ranking drops after a site update, or errors showing in Google Search Console. A technical SEO audit will reveal exactly what's wrong.",
  },
  {
    q: "Can technical issues really affect my rankings?",
    a: "Absolutely. Google prioritises sites that are fast, secure, mobile-friendly, and easy to crawl. Technical problems send negative signals that push you down in search results — even if your content is great.",
  },
  {
    q: "Do you work with developers to implement fixes?",
    a: "Yes. I can work alongside your developer or handle fixes directly if your site is on WordPress or another platform I can access. Either way, you'll get a clear action plan.",
  },
  {
    q: "How long does a technical SEO audit take?",
    a: "A comprehensive audit typically takes 3–5 business days, depending on the size of your website. You'll receive a detailed report with prioritised recommendations.",
  },
  {
    q: "Is technical SEO a one-time thing?",
    a: "Not entirely. While many fixes are one-time, technical issues can resurface after site updates, plugin changes, or new content. Ongoing monitoring ensures problems are caught early.",
  },
];

function FAQItem({ faq, index, inView }: { faq: typeof faqs[0]; index: number; inView: boolean }) {
  const [open, setOpen] = useState(false);

  return (
    <motion.div
      initial={{ opacity: 0, y: 16 }}
      animate={inView ? { opacity: 1, y: 0 } : {}}
      transition={{ duration: 0.4, ease: "easeOut", delay: 0.1 + index * 0.06 }}
      className="border border-gray-800 rounded-xl overflow-hidden
                 hover:border-orange-500/30 transition-colors duration-300"
    >
      <button
        onClick={() => setOpen(!open)}
        className="w-full flex items-center justify-between gap-4 p-5 bg-[#141414] text-left group"
      >
        <span className="text-white font-semibold text-sm group-hover:text-orange-400
                         transition-colors duration-300">
          {faq.q}
        </span>
        <span className={`text-orange-500 text-xl font-light shrink-0 transition-transform
                          duration-300 ${open ? "rotate-45" : "rotate-0"}`}>
          +
        </span>
      </button>

      <AnimatePresence initial={false}>
        {open && (
          <motion.div
            initial={{ height: 0, opacity: 0 }}
            animate={{ height: "auto", opacity: 1 }}
            exit={{ height: 0, opacity: 0 }}
            transition={{ duration: 0.28, ease: "easeInOut" }}
            className="overflow-hidden"
          >
            <div className="px-5 pb-5 bg-[#141414] border-t border-gray-800/60">
              <p className="text-gray-400 text-sm leading-relaxed pt-4">{faq.a}</p>
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </motion.div>
  );
}

export default function TechnicalSeoFAQ() {
  const ref = useRef(null);
  const inView = useInView(ref, { once: true, margin: "-80px" });

  return (
    <section className="bg-[#0a0a0a] py-24 px-6 md:px-16 lg:px-24">
      <div className="max-w-3xl mx-auto" ref={ref}>

        <motion.p
          initial={{ opacity: 0, y: 16 }}
          animate={inView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.5, ease: "easeOut" }}
          className="text-orange-500 text-sm font-bold tracking-widest uppercase mb-3 text-center"
        >
          FAQ
        </motion.p>

        <motion.h2
          initial={{ opacity: 0, y: 20 }}
          animate={inView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.55, ease: "easeOut", delay: 0.1 }}
          className="text-white text-3xl md:text-4xl font-extrabold leading-tight mb-14 text-center"
        >
          Technical SEO —{" "}
          <span className="text-orange-500">Frequently Asked Questions</span>
        </motion.h2>

        <div className="flex flex-col gap-3">
          {faqs.map((faq, i) => (
            <FAQItem key={faq.q} faq={faq} index={i} inView={inView} />
          ))}
        </div>
      </div>
    </section>
  );
}